---
# Example admin configuration file
admins:
  # Admin display name
  - displayName: "root"
    # Unique identifier for this admin
    uuid: "123e4567-e89b-12d3-a456-426614174000"
    # Platforms this admin is accepted on (regex patterns)
    acceptedPlatforms:
      - "irc"
      - "discord"
    # Authentication methods for this admin
    authentication:
      # Currently supporting IRC hostmask identification
      irc:
        hostmask: "nick!username@example.com"
# Rate limit configuration for admin commands
ratelimits:
  join:
    mode: drop
    level: user
    limit: 3
    interval: 1m
  part:
    mode: drop
    level: user
    limit: 3
    interval: 1m
  showRatelimits:
    mode: drop
    level: user
    limit: 3
    interval: 1m